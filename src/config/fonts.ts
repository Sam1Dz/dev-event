import { Martian_Mono, Schibsted_Grotesk } from 'next/font/google';

export const fontSchibstedGrotesk = Schibsted_Grotesk({
  variable: '--font-schibsted-grotesk',
  subsets: ['latin'],
});

export const fontMartianMono = Martian_Mono({
  variable: '--font-martian-mono',
  subsets: ['latin'],
});
